<h1>Editing operator</h1>

<% form_for(@operator) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @operator %> |
<%= link_to 'Back', operators_path %>